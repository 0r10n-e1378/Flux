package entities;

public class Boid extends Actor{

	public Boid (int xPos, int yPos, int radius, int acceleration, int health, int maxSpeed) {
		super(xPos, yPos, radius, acceleration, health, maxSpeed);
	}
	
}
