package entities;

public class Commander extends Actor{

	public Commander (int xPos, int yPos, int radius, int acceleration, int health, int maxSpeed) {
		super(xPos, yPos, radius, acceleration, health, maxSpeed);
	}
	
}
